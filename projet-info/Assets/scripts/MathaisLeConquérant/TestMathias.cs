using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestMathias : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        PathfindingMathias pathfinding = new PathfindingMathias(10, 10);
    }

}
